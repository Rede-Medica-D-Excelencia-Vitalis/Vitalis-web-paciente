// Arquivo principal que exporta todos os stores organizados por categoria
// Este arquivo permite imports limpos como: import { useAuthStore, useCartStore } from './store'

// Stores de autenticação
export * from './auth';

// Stores de negócio
export * from './business';
